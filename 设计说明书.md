# 设计说明书

## 基于特征工程与 LSTM 的设备故障 PHM 预测系统

> 班级：＿＿＿＿　　学号：＿＿＿＿　　姓名：＿＿＿＿（请自行填写）
>
> 制造智能技术课程设计 · 2026 年 9 月

---

## 一、背景调研（研究意义）

### 1.1 选题背景

设备故障预测与健康管理（PHM，Prognostics and Health Management）是智能制造里的一个热门方向，核心一句话：**用数据预测设备还能撑多久，把维修从"坏了再修"变成"快坏了提前修"**。

传统维护模式有两种。一种是事后维修，设备坏了才动手，一旦是航空发动机、机床主轴这类关键部件，一次非计划停机造成的生产损失和安全隐患都很大；另一种是定期维修，按固定周期保养，问题在于周期定长了容易漏、定短了造成过度维护，备件和人力都被浪费。业内一般认为，如果能把维护决策建立在状态监测数据的预测结果上，维护成本有机会降低 30%~50%（行业公开资料的普遍估计，具体数值因设备类型差异较大）。

### 1.2 相关研究现状

RUL（剩余使用寿命）预测方法大致走过三个阶段：基于物理模型的方法精度高，但需要精确的机理知识，工程上很难针对每台设备建模；基于统计模型的方法（如 Wiener 过程、隐马尔可夫模型）对数据分布有较强假设；近年来主流是数据驱动方法，其中 LSTM 因为能记忆时间序列的长程依赖，成为 RUL 预测的常用模型。Heimes 在 2008 年最早把循环神经网络用于 RUL 估计，后续大量研究在 NASA C-MAPSS 数据集上验证了 LSTM 相比传统机器学习方法的优势。

本设计采用"**特征工程 + LSTM 主模型 + 随机森林基线对照**"的技术路线，和主流研究思路保持一致，同时把课程里学过的数据处理、机器学习、深度学习三个专题真正落地到一个可运行的系统里。

### 1.3 研究意义

- **工程价值**：交付一个浏览器即用的 B/S 预测系统，用户上传传感器数据就能拿到 RUL 预测和健康等级，具备基本的可部署性；
- **技术价值**：RUL 预测是典型的多变量时间序列回归问题，把特征工程、机器学习、深度学习的知识串成一条完整链路；
- **可推广性**：虽然演示对象是航空发动机，但特征工程和预测方法可以直接迁移到机床主轴、风机、压缩机等旋转机械上。

---

## 二、方案设计

### 2.1 设计目标

搭建一个可运行的 B/S 架构 RUL 预测系统：用户上传设备传感器时序数据，系统返回剩余寿命预测值、健康等级和维护建议，并支持历史预测记录查询。系统要能在普通笔记本上跑起来，所以模型规模控制在 CPU 可训练的范围。

### 2.2 总体架构

系统采用分层 B/S 架构，各层通过 HTTP/JSON 接口解耦：

```
浏览器前端（HTML/CSS/JS + ECharts）
      │ HTTP/JSON
FastAPI 后端服务（路由 + 业务编排）
      ├── 算法层：特征工程 → LSTM 预测 → 健康评估（随机森林作基线对照）
      └── 数据层：SQLite（预测记录、单元信息）
```

用户侧零安装，打开浏览器访问本地服务即可使用；算法与展示分离，后续换模型或换前端都不需要动对方。

### 2.3 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 原生 HTML/CSS/JS + ECharts | 不需要构建链，部署简单；ECharts 图表库成熟稳定 |
| 后端 | Python + FastAPI + Uvicorn | 轻量、自带接口文档，异步性能好，适合课程规模 |
| 数据库 | SQLite | 零配置单文件，不需要单独装数据库服务 |
| 深度学习 | PyTorch（CPU 版） | 生态成熟，接口灵活，CPU 上也能完成训练 |
| 机器学习 | scikit-learn（随机森林） | 实现简单、可解释性好，作为基线模型 |
| 测试 | pytest + FastAPI TestClient | 算法和接口都能自动化验证 |

### 2.4 技术路线

```
原始时序数据 → 数据清洗 → 传感器筛选 → 滑动窗口 → 归一化
        → LSTM 训练（主模型）＋ 随机森林训练（基线）
        → 模型封装 → FastAPI 推理服务 → B/S 前端展示 → SQLite 持久化
```

设计上把随机森林放在"基线"位置而不是与 LSTM 平级，是为了让结果对比有说服力：同一个数据集、同一套特征，传统机器学习做到什么水平，LSTM 又做到什么水平，差距一目了然。

### 2.5 健康状态评估（辅助决策模块）

预测出 RUL 后，再映射为四级健康状态并给出维护建议，方便非专业用户直接做决策：

| RUL 区间 | 健康等级 | 维护建议 |
|---|---|---|
| > 100 | 健康 | 常规周期维护 |
| 50~100 | 退化 | 纳入计划性维护窗口 |
| 20~50 | 危险 | 近期安排检修，加密监测 |
| ≤ 20 | 失效风险 | 立即停运检修 |

---

## 三、数据集构建

### 3.1 数据来源

采用 **NASA C-MAPSS** 涡扇发动机退化仿真公开数据集（FD001 子集），这是 RUL 预测领域最常用的标准基准数据之一。数据由 NASA 官方提供（公开链接见仓库 README 与 data 目录说明）。

数据规模如下：

| 项目 | 训练集 train_FD001 | 测试集 test_FD001 |
|---|---|---|
| 发动机台数 | 100 台 | 100 台 |
| 数据行数 | 20 631 行 | 13 096 行 |
| 数据形态 | 完整生命周期（运行至失效） | 截断时序（只给中间一段） |
| RUL 标签 | 自行计算（最大循环 − 当前循环） | 官方真值 RUL_FD001.txt |

每条记录包含 3 个工况参数和 21 个传感器信号，单位是飞行循环（cycle）。原始数据量约几十 MB，直接提交在仓库 `data/raw/` 目录下，同时保留官方来源链接，满足任务书对公开数据集的要求。

### 3.2 数据划分

训练集和测试集是**完全不同的 100 台发动机**，按发动机划分、样本零重叠，不会出现"训练时见过这台设备"的数据泄漏。训练集负责学习完整退化过程，测试集模拟真实使用场景——设备还在运行、只有历史数据，模型要预测还能撑多久。

训练时又从训练窗口样本中随机抽取 15% 作为**验证集**，用于挑选最优模型权重和触发学习率衰减。测试集只在最终评估时用一次，不参与任何调参。

### 3.3 数据预处理

预处理脚本为 `scripts/preprocess.py`，一键复现，产物放在 `data/processed/`：

| 步骤 | 方法 |
|---|---|
| RUL 标签构造 | 训练集每循环 RUL = 该单元最大循环 − 当前循环，上限截断 125 |
| 传感器筛选 | 计算各传感器与 RUL 的 Pearson 相关系数，保留 \|r\| ≥ 0.5 的 12 个 |
| 滑动窗口 | 窗口长度 30、训练步长 3，构造 (30, 12) 的序列样本 |
| 归一化 | Min-Max 归一化到 [0, 1] |
| 统计特征 | 窗口内 12 传感器 × 4 统计量（均值/标准差/最大值/最小值）= 48 维 |

处理结果：训练窗口样本 5 947 条。数据量小、可直接入库，大体积的中间样本由脚本重建，不在 git 里存冗余副本。

---

## 四、三个技术方向的详细设计

### 4.1 技术方向一：数据处理与特征工程

**设计思路**：原始 21 个传感器里，有一部分是随退化几乎不变化的量（比如某些恒定温度、压力信号），直接全部喂给模型既增加计算量，又可能引入噪声。因此先做相关性筛选，把真正随设备退化变化的传感器保留下来。

**详细步骤**：

1. **数据清洗**：读取原始文件，剔除空值和格式异常的记录，统一列名；
2. **传感器筛选**：计算 21 个传感器与 RUL 的 Pearson 相关系数，保留 \|r\| ≥ 0.5 的 12 个传感器（sensor2、3、4、7、8、11、12、13、15、17、20、21），21 → 12；
3. **滑动窗口**：窗口长度 30 个循环、训练步长 3，把长时序切成 (30, 12) 的样本，每个样本对应一个 RUL 标签；测试时取每台发动机最后 30 个循环作为输入；
4. **归一化**：Min-Max 归一化到 [0, 1]，统一量纲，避免大数值传感器主导梯度，加速收敛；
5. **RUL 截断**：把训练标签上限截断到 125，避免"刚出厂就预测 200 个循环"这类长尾对训练干扰过大（文献中普遍做法）。

**关键实现**：特征提取函数 `last_window_for_unit(df, sensor_cols, scaler, window=30)` 负责为单台测试发动机取最后一个窗口并完成归一化；短序列（不足 30 个循环）做前向补齐，保证接口不报错。

**设计取舍**：窗口 30、步长 3 是试出来的折中——窗口太短捕捉不到退化趋势，太长样本量骤减；步长 3 让相邻样本有一定重叠，既保留信息量又不至于样本量爆炸。

### 4.2 技术方向二：机器学习——随机森林基线

**设计思路**：随机森林代表"传统机器学习"这条技术路线。它不直接处理时序，而是把窗口统计成特征再回归，作为对照可以回答一个问题：**不显式建模时间顺序，光靠统计特征能做到什么水平？**

**实现细节**：

- 原理：用 bootstrap 有放回抽样生成多棵决策树，每棵树随机挑选特征子集独立训练，回归任务取所有树预测的均值，从而降低单棵树的过拟合；
- 参数：`n_estimators=200`（200 棵树）、`max_depth=20`；
- 输入特征：每个窗口内 12 个传感器统计均值、标准差、最大值、最小值，共 48 维；
- 训练与推理：`scripts/train.py` 训练，`joblib` 持久化，后端加载后直接推理。

**基线意义**：随机森林参数少、训练快、结果可复现，作为对照基线非常合适。最终结果也印证了预期——它在测试集上 RMSE 为 48.90，明显差于 LSTM（详见 4.3），说明只靠窗口统计特征、丢掉时间顺序信息，退化预测的精度会受到明显限制。

### 4.3 技术方向三：深度学习——LSTM 预测模型

**设计思路**：设备退化是一个随时间累积的过程，当前时刻的健康状态和过去几十个循环的趋势强相关。LSTM 通过门控机制记忆长程信息，天然适合这类时序回归任务，所以选它做主模型。

**网络结构**（`RulLSTM`）：

```
输入 (batch, 30, 12)
  → LSTM 层1（128 个隐藏单元）
  → LSTM 层2（128 个隐藏单元，dropout 0.2）
  → 特征拼接：最后时间步输出 ⊕ 全局均值池化 → 256 维
  → 全连接层 → RUL 输出
```

拼接"最后时间步 + 全局均值"是这一步的关键设计：最后时间步保留**近期**的退化细节，全局均值体现整个窗口的**整体**退化水平，两者互补。

**训练策略**：

| 项目 | 配置 |
|---|---|
| 损失函数 | MSE（均方误差） |
| 优化器 | Adam，学习率 0.001，带学习率衰减 |
| 训练轮数 / 批次 | 60 轮 / batch 128 |
| 防止过拟合 | dropout 0.2 + 验证集早停（保存验证误差最小权重） |
| 验证集 | 训练样本随机抽 15% |

**推理流程**：测试时取发动机最后 30 个循环，经同样的归一化和窗口处理输入模型，输出即 RUL 预测值；模型权重和预处理参数一起打包，后端加载后单次推理在 CPU 上不足 1 秒。

**与基线对比**：

| 模型 | 测试集 RMSE |
|---|---|
| LSTM（主模型） | 15.95 |
| 随机森林（基线） | 48.90 |

LSTM 比随机森林低约 67%，验证了显式时序建模在退化预测上的优势。训练过程验证集 RMSE 为 13.85，测试集 15.95，两者差距不大，说明没有明显过拟合。

---

## 五、界面设计

### 5.1 设计原则

界面按"少而清晰"的原则设计：功能只有四个区，不堆砌；结果用颜色和等级标签直接表达，非专业用户也能看懂。系统是 B/S 架构，浏览器打开即用，免安装。

### 5.2 功能分区

1. **数据上传区**：文件选择按钮 + 设备编号输入框，支持 C-MAPSS 原始格式和带表头的 CSV；
2. **预测结果区**：展示 LSTM 预测 RUL、随机森林对照值、健康等级徽章（颜色区分）和维护建议；
3. **趋势可视化区**：用 ECharts 折线图展示最近窗口内关键传感器的原始值趋势，让预测结果"看得见"；
4. **历史记录区**：表格展示 SQLite 中的预测记录，可随时回看之前的预测。

### 5.3 交互流程

```
选择文件 → 输入设备编号（可选）→ 点击"开始预测"
  → 后端校验文件 → 特征工程 → LSTM/RF 推理 → 健康评估 → 写入 SQLite
  → 前端展示 RUL、健康等级、建议与趋势图 → 历史记录可查询
```

页面实际运行效果见答辩 PPT 及系统截图（本地运行于 http://127.0.0.1:8000）。

---

## 六、调试

这部分记录开发过程中实际遇到的三个比较典型的问题，都是真实踩过的坑。

### 6.1 环境问题：Anaconda 基础环境依赖冲突

项目初期直接用 Anaconda 基础环境跑，结果 import pandas 就崩溃。排查发现是基础环境里 numpy 1.x 时代编译的库和后来装上的 numpy 2.x 混装在一起，版本互相不兼容。处理办法是放弃基础环境，重建一个干净的虚拟环境，numpy/pandas/scikit-learn 走 PyPI 安装，PyTorch 单独走 CPU 源安装，彻底隔离。

### 6.2 数据问题：官方下载源失效

C-MAPSS 数据集的多个官方下载地址当时都打不开（官网链接失效、部分镜像返回错误页）。最终改用 GitHub API 直接拉取数据文件（按 blob 接口获取 base64 内容），下载完成后核对了行数——训练集 20 631 行、测试集 13 096 行，与官方说明一致，确认数据完整无误后入库。

### 6.3 代码问题：函数参数顺序错误

特征提取函数在定义时参数顺序（scaler 与 window 的位置）和调用处不一致，运行时直接抛 TypeError。用最小复现脚本定位到定义处和调用处的签名不一致后，统一了函数签名并重新跑了一遍预处理，同时补了对应的单元测试，防止以后再改坏。

### 6.4 自动化测试

用 pytest 写了 14 个用例，覆盖三层：

- 特征工程层：数据加载、RUL 计算、传感器筛选、窗口形状、短序列补齐；
- 模型层：LSTM 前向传播与推理、随机森林推理；
- 接口层：健康检查、页面访问、预测接口（样例数据）、空文件处理、历史接口。

14 个用例全部通过（模型未训练时相关用例自动跳过，保证测试随时可运行）。

### 6.5 端到端验证

编写了一键启动脚本 `start.bat`：自动建环境 → 数据预处理 → 模型训练 → 启动服务，双击即可运行。用 `data/sample_upload.csv`（5 号测试发动机 98 个循环）完整走通"上传 → 预测 → 展示 → 入库 → 历史查询"全流程，实测 5 号发动机预测 RUL 101.14（官方真值 91），单次预测响应小于 1 秒。

---

## 七、总结与展望

### 7.1 工作总结

本设计完成了从选题、数据准备、算法开发到 B/S 系统集成的完整课程设计：

- 覆盖三个课程技术方向（数据处理与特征工程、机器学习随机森林、深度学习 LSTM），每个方向都在系统里承担实际功能并有量化结果；
- 系统可一键启动、现场演示，自动化测试 14 项全部通过；
- 全程使用 git 小步提交管理代码，AI 辅助开发的过程记录（prompt 会话）按阶段备份在 prompt 目录，可追溯。

### 7.2 存在的不足

- 只在 FD001 单工况数据上验证，未覆盖多工况（FD002~FD004），泛化能力还没有充分检验；
- 前端是单页原生实现，功能偏演示，距离生产级系统还有差距；
- 模型只用了 LSTM，没有尝试注意力机制等更新结构，精度提升空间还在。

### 7.3 展望

后续可以往三个方向走：一是引入多工况数据做迁移学习，提升跨场景泛化能力；二是尝试注意力机制或 Transformer 结构，建模更长的退化依赖；三是做流式在线预测与阈值告警，把系统真正接到实时运维场景里。

---

## 参考文献

[1] 张智海 等. 制造智能技术基础[M]. 北京: 清华大学出版社, 2022.

[2] 杨杰. 人工智能基础[M]. 北京: 机械工业出版社, 2025.

[3] Saxena A, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation[C]. International Conference on Prognostics and Health Management, 2008.

[4] Hochreiter S, Schmidhuber J. Long short-term memory[J]. Neural Computation, 1997, 9(8): 1735-1780.

[5] Heimes F O. Recurrent neural networks for remaining useful life estimation[C]. International Conference on Prognostics and Health Management, 2008.

[6] NASA PCoE. C-MAPSS dataset[DB/OL]. https://data.nasa.gov/dataset/cmapss-jet-engine-simulated-data
