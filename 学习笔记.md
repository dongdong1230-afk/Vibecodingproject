# 学习笔记

> 课程设计阶段 1 学习记录
> 内容：AI 工具（vibe coding）学习、拟选用 harness 与模型、git 原理与功能、选题调研
> 更新时间：2026-09-11

---

## 一、AI 工具学习：vibe coding 方法论

### 1.1 什么是 vibe coding

vibe coding 是一种"以自然语言驱动编程"的开发方式：开发者用自然语言向 AI 编程助手描述需求、约束和验收标准，由 AI 生成或修改代码，开发者负责拆解任务、把关设计、审查代码并完成集成。它与传统编程的区别在于：**开发者的核心工作从"逐行写代码"转变为"提需求、定边界、做验收"**。

本课程设计全程采用 vibe coding 方法开发，重点学习了以下五个方面。

### 1.2 提示词工程

提示词是驱动 AI 编程的核心输入，本次实践中总结出三类关键要素：

| 要素 | 说明 | 示例 |
|---|---|---|
| 任务拆解 | 把大需求拆成可单步完成的小任务，一次让 AI 只做一件事 | 先"设计数据库表结构"，再"实现训练脚本"，而不是一次要整个系统 |
| 约束说明 | 明确技术栈、框架版本、文件路径、命名规范等硬约束 | "使用 Python 3.12 + FastAPI，数据库用 SQLite，不要引入 Docker" |
| 验收标准 | 告诉 AI "做到什么程度算完成"，可量化、可验证 | "接口返回 JSON，字段为 id/unit/predicted_rul/health_level，需通过 pytest" |

实践体会：提示词越具体，AI 输出的可用率越高；把"目标+约束+验收"写全，能显著减少反复修改的轮次。

### 1.3 上下文工程

AI 编程助手在长项目中容易"失忆"，本次学习采用以下做法保持上下文一致：

- **项目规则文件**：在仓库根目录维护 `CLAUDE.md`/`AGENTS.md` 之类的规则文件，写清项目结构、技术栈、代码风格，让 AI 每次开工前先读取；
- **让 AI 先读代码再改**：任何修改前先让 AI 读取相关文件，避免凭印象改出与现有代码冲突的结果；
- **prompt 日志备份**：与 AI 的每次关键对话保存为 JSON 文件放入 `prompt/` 目录，压缩上下文前及时备份，保证过程可追溯（本仓库 `prompt/` 目录即为此设立）。

### 1.4 规格驱动开发（Spec-Driven Development）

按"需求文档 → AI 制定计划 → 分步实现 → 逐步验收"的流程推进：

1. 先写《需求规格说明书》和《方案设计》，把功能、接口、数据结构定下来；
2. 让 AI 根据规格输出实现计划和文件清单；
3. 按模块小步实现，每完成一个模块立即运行对应测试验收；
4. 验收不过就回滚或缩小改动，不带着未验证的代码继续堆功能。

### 1.5 AI 代码的自动化测试与人工审查

- **自动化测试**：为特征工程、模型推理、API 接口编写 pytest 用例，AI 生成的代码必须能通过测试才算完成；
- **人工审查**：AI 生成的关键代码（模型结构、数据加载、接口逻辑）逐段人工阅读，确认理解每一行的作用——这一点直接关系到答辩时能否讲清楚 AI 生成的代码（任务书要求：老师会随机指定 1-2 处 AI 生成代码现场讲解）。

### 1.6 调试与人工接管策略

- **回滚**：小步提交（git）保证任何一次失败都能回退到可用版本；
- **缩小改动**：出问题时先定位到最近一次改动，只修复最小范围，不整体重写；
- **最小复现**：把报错场景压缩成最小可复现用例（如单条数据、单个接口）再让 AI 修复，避免在大工程里盲目尝试。

---

## 二、拟选用的 harness 与模型

### 2.1 Harness（AI 编程助手）调研

任务书列出的候选 harness 及对比：

| Harness | 特点 | 适配本项目的考量 |
|---|---|---|
| Trae | 集成 IDE 形态，支持多模型切换，中文友好 | 上手快，适合课程设计 |
| ZCode | 云端开发环境 + AI 编码 | 需要网络稳定 |
| Qwen Code | 阿里通义系，代码能力强，免费额度充足 | 可用 |
| 通义灵码 | IDE 插件形态，与 VSCode/JetBrains 集成 | 可用 |
| Deepseek Harness | 推理能力强的模型配套工具 | 模型较强 |
| Claude Code | 命令行形态，代码能力业界领先 | 需科学网络，不稳定 |
| Codex | OpenAI 官方编码工具 | 需付费/网络条件 |
| Open Code | 开源编码终端 | 配置成本高 |

**选用结论**：本项目实际使用**豆包 AI 助手**作为主 harness（桌面端集成、上下文管理方便、对话记录可导出为过程档案），以 **Trae** 和 **Deepseek Harness** 作为备选对照工具。课程设计中保留全部对话记录到 `prompt/` 目录，保证 AI 使用过程可追溯、可在答辩中如实披露。

### 2.2 编程大模型对比

| 模型 | 特点 | 本项目使用 |
|---|---|---|
| Deepseek-v4 | 推理与代码能力强，中文支持好 | 备选 |
| GLM-5.3 | 智谱系，工具调用稳定 | 备选 |
| Qwen 3.8 | 阿里系，上下文长，免费 | 备选 |
| Kimi K3 | 长上下文，中文好 | 备选 |
| Claude Code | 代码生成质量高 | 备选 |
| 豆包内置大模型 | 与豆包助手一体化，对话连续性最好 | **主用** |

选用理由：豆包内置模型与助手一体化，上下文连续性好，适合长时间、多轮次的 vibe coding 会话；代码生成质量满足本项目（FastAPI 后端、前端页面、LSTM 训练脚本）的需求。

### 2.3 领域模型说明

本项目的**算法模型**（LSTM 剩余寿命预测）由我们基于 PyTorch 自行设计训练，不属于"编程大模型"，两者分工：

- **编程大模型**：负责生成业务代码（API、前端、数据处理脚本）；
- **算法模型**：LSTM 网络结构、特征工程流程由我们设计、训练并验证，模型权重文件由训练脚本产出。

---

## 三、git 原理与功能

### 3.1 git 是什么

git 是一个**分布式版本控制系统**，每个开发者的本地仓库都包含完整的历史记录，不依赖中心服务器。本课程设计全程使用 git 管理代码，要求小步提交、提交信息有意义。

### 3.2 核心原理：三个区域

```
工作区（Working Directory）  →  git add  →  暂存区（Staging Area）  →  git commit  →  本地仓库（Repository）  →  git push  →  远程仓库（Remote）
```

- **工作区**：当前能看到的文件；
- **暂存区（index）**：`git add` 后暂存的文件快照；
- **本地仓库**：`git commit` 后真正保存的历史版本（.git 目录）。

每次 commit 保存的是一棵文件树快照 + 作者信息 + 提交说明，形成一条不可变的历史链。

### 3.3 常用命令

| 命令 | 功能 |
|---|---|
| `git clone <url>` | 克隆远程仓库到本地 |
| `git status` | 查看工作区/暂存区状态 |
| `git add <file>` | 把文件加入暂存区 |
| `git commit -m "说明"` | 提交一个版本 |
| `git log --oneline` | 查看提交历史 |
| `git branch` / `git checkout -b <b>` | 查看/新建分支 |
| `git merge <b>` | 合并分支 |
| `git push` / `git pull` | 推送/拉取远程仓库 |
| `git diff` | 查看改动内容 |
| `git reset` / `git revert` | 回退（回滚策略） |
| `git rm` | 删除文件并记录删除 |

### 3.4 分支与协作模型

本项目采用**单主分支（main）+ 小步提交**的轻量模型：功能简单、单人开发，不需要复杂分支策略；每次改动独立成 commit，提交信息描述"做了什么、为什么"。

### 3.5 本项目 git 使用规范

1. 小步提交：一个功能一个 commit，禁止大杂烩提交；
2. 提交信息格式：`<类型>: <简述>`，如 `docs: 新增学习笔记`、`feat: 实现 LSTM 训练脚本`、`test: 新增 API 测试`、`data: 更新预处理数据`；
3. `.gitignore` 排除虚拟环境、模型权重、中间产物等不该入库的文件；
4. 关键节点（每阶段完成）推送远程仓库，远程仓库即考核依据。

---

## 四、选题调研

### 4.1 领域背景

设备故障预测与健康管理（PHM, Prognostics and Health Management）是智能制造的重要方向：通过传感器数据预测设备剩余使用寿命（RUL, Remaining Useful Life），提前安排维护，避免非计划停机。PHM 属于制造环节中"设备运维"的智能化应用，场景真实、数据集公开，适合课程设计。

### 4.2 数据集调研：NASA C-MAPSS

| 项目 | 内容 |
|---|---|
| 数据来源 | NASA 卓越预测中心（PCoE）公开数据集 |
| 数据内容 | 涡扇发动机退化仿真数据，多变量时间序列 |
| 规模 | 训练/测试各 100 台发动机（FD001），每台多个飞行循环 |
| 特征 | 3 个工况参数 + 21 个传感器测量值 |
| 任务 | 根据传感器时序预测发动机剩余寿命（回归） |
| 来源链接 | https://data.nasa.gov/dataset/cmapss-jet-engine-simulated-data |

该数据集被广泛用于 PHM/RUL 预测研究，具有**公开、规模适中、任务明确**的优点，适合本项目。

### 4.3 技术方法调研

- **特征工程**：原始 21 个传感器并非全部包含退化信息，需先做相关性/趋势分析筛选有效传感器，再构造滑动窗口统计特征（均值、标准差、斜率等）——对应课程中数据预处理与特征提取章节；
- **LSTM 循环神经网络**：RUL 预测本质是时间序列预测，LSTM 能捕获长期依赖，是 PHM 领域的经典方法——对应课程中深度学习/循环神经网络章节；
- **机器学习基线**：用随机森林/线性回归等经典模型作为对照——对应机器学习章节；
- **健康状态评估**：将预测 RUL 映射为健康等级（健康/退化/危险/失效），支撑维护决策——对应状态评估与决策章节。

### 4.4 选题结论

综合以上调研，确定选题为：

**《基于特征工程与 LSTM 的设备故障 PHM 预测系统》**

- 场景：设备运维（制造/工业环节，符合选题要求）；
- 技术方向覆盖：机器学习、深度学习（LSTM）、数据预处理与特征工程、可视化与状态评估（≥3 个课程技术方向）；
- 数据：NASA C-MAPSS 公开数据集；
- 架构：B/S 架构（前端 Web UI + 后端 API + 数据库 + 算法模块）。

详见《选题说明.md》与《方案设计.md》。
