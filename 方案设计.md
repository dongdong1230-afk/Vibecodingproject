方案设计
1 功能需求分析
1.1 用户角色
1. BOM工程师：录入/导入产品物料清单，查看BOM解析结果，识别风险物料，查看物料替代候选方案；
2. 工艺工程师：查看多套替代方案评估结果，对比原BOM与修改后BOM差异；
3. 系统管理员：维护物料知识库、历史BOM变更案例库。

1.2 功能需求
 基础知识库管理
维护物料基础信息：物料编号、物料类别、关键性能参数、采购成本、标准交期、物料状态（正常供货/缺货/停产）；维护物料本体约束规则；维护历史BOM变更案例库。

BOM智能解析
支持手动录入BOM或导入样例BOM数据；解析多级BOM，生成可视化产品物料结构树；识别BOM内全部物料，标记缺货、停产、交期超期的风险物料。

物料替代知识推理
调用知识推理模块，结合物料本体约束，筛选满足兼容条件的候选替代物料；排除性能、工艺不匹配的物料。

CBR历史案例检索
针对风险物料检索历史BOM变更案例，返回历史上同类物料替换的成功案例，作为工程师参考。

替代方案多维度综合评估
使用模糊综合评价算法，对多套候选物料替换方案进行打分；评估维度包括采购成本、供货交期、性能匹配、工艺兼容性、历史故障情况；输出方案排序，展示每套方案各项指标得分。

BOM方案管理
保存原始BOM、替换修改后的BOM方案；支持不同版本BOM对比；支持历史BOM方案查询回溯。

1.3 非功能需求
1. B/S架构，浏览器访问；
2. SQLite完成业务数据持久化；
3. 三大算法模块独立，支持单元测试；
4. 打通完整业务闭环；
5. Vibe‑Coding开发范式；Git小步多次提交；完整留存prompt会话过程档案。

2 方案论证
传统BOM物料变更完全依靠工程师经验：BOM层级多，人工查找替代物料工作量大，容易忽略兼容性约束；缺少历史变更案例复用，替换方案缺少多维度量化对比。
本系统融合三项制造智能技术：
1. 物料本体知识推理：定义物料之间的兼容与替代规则，自动过滤不满足条件的候选物料；
2. 基于案例推理CBR：复用历史经过验证的BOM物料替换案例；
3. 模糊综合评价：对多套替换方案做量化打分排序，辅助人员选出综合更优方案。
系统输出仅作为工程师的辅助参考，最终BOM变更必须由BOM/工艺工程师确认。

3 方案规划
3.1 技术栈
- 后端：Python Flask，提供REST‑API接口
- 前端：HTML+JavaScript+CSS；实现BOM树可视化、表格展示、方案对比页面
- 数据库：SQLite
- 算法模块：Python实现本体规则推理、CBR案例检索、模糊综合评价
- 版本管理：Git+GitHub
- AI辅助开发：DeepSeek‑v4、通义灵码；采用Vibe‑Coding规格驱动开发

3.2 外部资源与数据集来源
1. 公开制造仿真BOM数据集；
2. 开源物料本体、零部件参数公开数据集；
对原始数据集做清洗、规整；补充仿真BOM变更案例，导入数据库，用于系统演示，无需企业私有数据。

3.3 B/S四层系统架构
1. 前端UI层：BOM录入/导入页面、BOM结构树可视化页面、风险物料列表、替代方案展示页面、方案对比页面、历史BOM查询页面。
2. 后端服务层：Flask后端接口，接收前端请求，调度三大算法模块，读写数据库，返回JSON业务结果。
3. 算法模块层
    - 物料本体知识推理模块（物料替代约束推理）
    - CBR历史BOM变更案例检索模块
    - 模糊综合评价‑替代方案多维度评估模块
4. 数据持久层SQLite
数据表：物料基础信息表、物料本体规则表、BOM主表、BOM子项明细表、风险物料记录表、BOM变更案例库表、BOM版本方案表。

3.4 核心API规划
1. POST /api/bom/upload：录入/上传产品BOM，执行解析
2. GET /api/bom/tree/{bom_id}：获取BOM结构化物料树
3. GET /api/bom/risk/{bom_id}：识别并返回BOM中的风险物料
4. POST /api/reasoning/substitute：调用知识推理，输出候选替代物料
5. GET /api/case/search：检索BOM变更历史相似案例
6. POST /api/eval/scheme：对多套替代方案执行模糊综合评价打分排序
7. POST /api/bom/save‑version：保存BOM修改版本
8. GET /api/bom/history：查询历史BOM版本记录

4 技术路线
1. 获取公开BOM、物料本体数据集，数据清洗，完成数据库表设计与建表；
2. 分别开发三大核心算法模块，编写单元测试；
3. 开发Flask后端全部API，完成算法模块集成；
4. 开发前端页面，实现BOM树可视化，前后端联调，打通完整业务流程；
5. 系统集成测试，修复bug，达到可现场演示状态；
6. 完善全部项目文档，归档prompt会话日志，整理课程设计报告素材。

5 计划安排（严格匹配任务书10天进度）
- D1‑D2：Vibe‑Coding学习、Git环境配置、选题调研；完成选题说明.md、方案设计.md编写并推送到远程仓库
- D3‑D5：数据集预处理，完成数据库建表，导入测试物料与BOM样例数据
- D6：三大算法模块编码实现，编写单元测试
- D7‑D8：后端API开发、前端UI开发、前后端联调
- D9：集成调试，完善README、prompt过程档案，撰写课程设计报告
- D10：录制3分钟演示视频，制作答辩PPT，完成答辩

 6 参考文献
[1] 张智海.制造智能技术基础[M].北京：清华大学出版社，2022.
[2] 杨杰.人工智能基础[M].北京：机械工业出版社，2025.
[3] 周济.本体技术在制造业BOM知识管理中的应用研究.制造业自动化.
[4] 公开制造业仿真BOM开源数据集.

7 本阶段验收标准
1. 仓库根目录存在选题说明.md、方案设计.md；
2. Git仓库存在多次有意义的commit提交记录；
3. 文档明确3项制造智能技术以及每项技术在系统中的实际业务作用，差异化说明完整；
4. 数据集来源、B/S架构、技术路线、时间规划、参考文献内容齐全。