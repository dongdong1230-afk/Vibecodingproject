# 制造智能技术课程设计——方案设计

面向离散制造业产品BOM智能解析与物料替代方案推理B/S系统

## 1 技术方向映射表

| 技术方向 | 课程技术模块 | 系统业务作用（非装饰，真实参与计算） |
|---|---|---|
| 本体与知识推理 | BOM-物料知识推理 | 构建物料本体知识库（类别层级、性能参数 schema、兼容约束、替代适配规则），解析 BOM 结构，推理物料间替代约束是否满足，过滤不兼容候选，输出可用替代物料集合——解决"哪些物料可以互相替代"的核心问题 |
| 案例推理 CBR | 历史 BOM 变更案例复用 | 建立历史 BOM 变更案例库（旧物料、替换物料、产品工况、变更原因、变更后效果），风险物料出现时按加权 kNN 检索相似案例，复用已验证有效的替换方案，并对历史失败案例给出警示 |
| 模糊综合评价 FCE | 替代方案多维度评估 | 对多套候选替代方案，综合采购成本、供货交期、性能匹配度、工艺兼容性、历史故障率 5 个指标，经隶属度函数、AHP+熵权组合权重、模糊合成算子打分排序，筛选综合最优 BOM 替换方案 |

## 2 功能需求分析

### 2.1 面向用户

- **BOM 工程师**：核心用户。导入/维护产品 BOM，查看风险物料，生成与评估替代方案，应用方案并回溯历史版本。
- **工艺工程师**：查看替代物料性能与工艺兼容性评估，参与方案确认。
- **采购工程师**（扩展）：查看替代物料成本、交期、供应商信息对比。

### 2.2 功能模块

| 模块 | 功能 | 说明 |
|---|---|---|
| BOM 管理 | 导入（Excel/CSV/JSON）/手工录入产品 BOM，结构化解析，构建多层物料结构树 | 自动推导层级、父件-子件-用量关系，支持单台用量自底向上汇总 |
| 风险识别 | 识别 BOM 中缺货、停产、交期超期风险物料 | 规则引擎判定，输出风险类型/级别/判定明细（可解释） |
| 替代推理 | 本体推理生成合规候选集 + CBR 检索历史验证案例 | 候选物料逐属性对比、推理路径可追溯；历史失败案例警示 |
| 方案评估 | FCE 五指标模糊综合评价，多方案打分排序 | 成本/交期/性能匹配/工艺兼容/故障率，输出评语等级与指标明细 |
| 方案对比 | 原 BOM 与替换后 BOM 逐项对比 | 物料、单价、交期、库存状态、故障率差异，成本/性能/采购风险三维对比 |
| 方案保存与回溯 | 方案保存、应用、BOM 版本快照、历史回溯与回滚 | 应用方案自动生成新版本；版本 diff 高亮；支持回滚 |
| 数据看板 | 风险统计、方案统计、案例效果分布等图表 | ECharts 可视化，支撑管理决策 |

## 3 方案论证

### 3.1 技术可行性

- **数据层面**：公开的真实企业级 BOM 数据集稀少（BOM 属企业核心数据）。采用"风格参考公开来源 + 脚本仿真生成"：BOM 字段结构参考公开制造 BOM 仿真数据集，物料件号体系参考 Octopart Common Parts Library（每个通用件至少 2 个可订购替代型号的思想与替代料业务天然契合），物料类别组织参考工业本体（IOF）与 ECLASS 分类思想。仿真数据由 scripts 按领域参数范围生成，刻意植入停产/缺货/长交期物料形成演示闭环。
- **算法层面**：三个模块均从零轻量实现——本体推理为 JSON 本体 + 规则引擎（与 OWL 类公理/SWRL 规则有明确对应关系，便于论述与答辩）；CBR 为加权 kNN 相似检索；FCE 为隶属度函数 + AHP/熵权权重 + 模糊合成，均为经典可解释算法，不依赖重型框架，离线可运行。
- **工程层面**：B/S 架构成熟，前端 Vue3 + Element Plus 免构建 SPA（本地 CDN 库，离线可用），后端 Python FastAPI + SQLite，算法封装为独立 service 层，参照上一课程设计（注塑成型系统）已验证的开发模式。

### 3.2 方案对比

| 对比维度 | 本方案（BOM 智能替代推理） | 人工查手册/凭经验 | 商业 PLM 插件 |
|---|---|---|---|
| 替代料检索效率 | 高（本体倒排索引秒级） | 低（逐项翻目录） | 中（依赖配置） |
| 评估维度 | 5 维定量打分可追溯 | 1~2 维定性 | 多依赖定制 |
| 历史经验复用 | CBR 自动检索相似案例 | 依赖个人记忆 | 无 |
| 成本 | 低（开源工具+本地部署） | 低 | 高（授权费） |
| 中小企业适用性 | ★★★★★ | ★★ | ★★★ |

## 4 方案规划

### 4.1 系统架构草图

```
┌──────────────────────────── 浏览器 (前端 SPA, Vue3+Element Plus+ECharts) ───────────────────────────┐
│  BOM管理 │ 风险识别 │ 替代推理 │ 方案对比 │ 方案历史 │ 数据看板                                       │
└───────────────────────────────┬──────────────────────────────────────────────────────────────────────┘
                                 │ REST API (/api/*)
┌───────────────────────────────▼──────────────────────────────────────────────────────────────────────┐
│                                  FastAPI 后端 (app/main.py)                                           │
│  routers: bom │ risk │ substitution │ ontology │ cases │ dashboard                                    │
│  ┌───────────────────────────────── services ─────────────────────────────────┐                       │
│  │ bom_service(解析/树构建/版本)   risk_service(规则判定)                        │                       │
│  │ substitution_service(业务闭环编排)                                           │                       │
│  │  ┌── ontology_service ──┬── cbr_service ──┬── fce_service ──┐               │                       │
│  │  │ 本体推理(候选生成+    │ 案例检索(加权   │ 五指标模糊综合   │               │                       │
│  │  │ 约束过滤+推理路径)   │ kNN+效果加权)   │ 评价+排序        │               │                       │
│  │  └──────────────────────┴─────────────────┴──────────────────┘               │                       │
│  │  rule_engine(JSON 规则表达式求值, 风险判定与本体规则共用)                       │                       │
│  └──────────────────────────────────────────────────────────────────────────────┘                       │
│  db.py: SQLAlchemy → SQLite (materials/bom_products/bom_lines/risk_records/                          │
│         bom_change_cases/substitution_plans/plan_scores/bom_versions)                                │
└───────────────┬──────────────────────────────────────────────────────────────────────────────────────┘
                │ 读取                                    │ seed_all.py 生成
┌───────────────▼───────────────┐          ┌──────────────▼───────────────┐
│ knowledge/ (提交入库, 知识)     │          │ data/processed/ (仿真数据)     │
│ material_ontology.json(本体)   │          │ materials.json/boms.json/     │
│ risk_rules.json(风险规则)      │          │ cases.json/bom.db             │
│ fce_config.json(FCE配置)       │          └──────────────────────────────┘
└───────────────────────────────┘
```

### 4.2 技术栈

| 层级 | 技术选型 | 说明 |
|---|---|---|
| 前端 UI | Vue3 + Element Plus + ECharts + axios | 免构建 SPA，全部库本地化（离线可用） |
| 后端服务 | Python FastAPI | RESTful API，承载业务逻辑与算法调用 |
| 算法模块 | 从零实现：本体规则引擎、加权 kNN、模糊综合评价 | 不依赖 owlready2/rdflib/sklearn 等重型框架，可解释性强 |
| 数据库 | SQLite（SQLAlchemy ORM） | 8 张业务表；DB_URL 可切 PostgreSQL |
| 开发工具 | VS Code + Claude Code（vibe coding） | 全程 AI 辅助，小步提交 |
| 版本控制 | Git | 全程小步提交，保留完整提交历史 |

### 4.3 数据来源与构建方案

- **物料本体库**（knowledge/material_ontology.json）：类别层级与属性 schema 参照工业本体（IOF Core，https://github.com/iofoundry/Core ）与 ECLASS（https://www.eclass.eu/ ）分类思想手工设计；物料件号编码体系参照 Octopart Common Parts Library（https://github.com/octopart/CPL-Data ）。
- **仿真 BOM 数据集**（scripts 生成）：字段结构参照公开制造 BOM 仿真数据集（如 Kaggle "Engineering Blueprints & BOM Dataset"，https://www.kaggle.com/datasets/devp1866/engineering-blueprints-and-bill-of-materials-dataset ）；8 个典型离散制造产品（智能电表、变频器控制板、AGV 驱动器、工业网关、伺服控制器、PLC 扩展模块、温控仪、步进驱动器）3~4 层 BOM。
- **历史变更案例库**（scripts 生成）：100 条案例，新物料保证是旧物料的合规替代（与本体推理一致）。
- **真实性声明**：以上均为脚本生成的仿真数据（随机种子固定可复现），非企业真实数据；文档与报告如实披露。

### 4.4 前端 UI 方案（6 个页面）

1. **BOM 管理**：产品列表 + 上传导入（模板下载、解析警告）+ 手工录入 + 多层物料结构树（用量汇总、风险色点）
2. **风险识别**：一键扫描 → 风险表格（类型/级别/判定明细展开）→ 一键跳转替代推理
3. **替代推理**：三栏布局（风险物料 / 候选物料卡片：本体候选参数对比+推理路径、CBR 案例卡+相似度+失败警示 / 已选清单 → FCE 评估 → 得分排序表 + 五指标雷达图 → 保存方案）
4. **方案对比**：原 BOM vs 替换后 BOM 逐项对比 + 图表 + 应用方案
5. **方案历史**：方案列表 + BOM 版本时间线 + 版本 diff + 回滚
6. **数据看板**：风险分布、FCE 得分趋势、成本对比、案例效果分布、命中规则 Top5

### 4.5 后端算法方案（三模块协同）

| 算法模块 | 技术实现 | 输入 | 输出 | 业务环节 |
|---|---|---|---|---|
| 本体与知识推理 | JSON 本体 + 规则引擎（三级过滤：类别约束→参数覆盖→显式规则） | 风险物料（类别路径+属性） | 合规替代集（含命中规则与推理路径） | 候选生成 |
| 案例推理 CBR | 加权 kNN（类别 Wu-Palmer + 参数相对容差 + 工况匹配，效果加权） | 风险物料+产品工况 | 可复用历史方案 + 失败警示 | 历史证据 |
| 模糊综合评价 FCE | 5 指标隶属度 + AHP/熵权组合权重 + M(·,+) 合成 | 候选方案指标矩阵 | 综合得分排序 + 评语等级 + 指标明细 | 方案择优 |

协同流程：用户选定风险物料 → 本体推理输出合规候选集（硬约束过滤）→ CBR 检索历史案例，把"已验证有效"的替换物料注入候选集（软证据）→ FCE 对候选∪原物料做五指标评价排序 → 工程师选择方案保存/应用 → 应用后实际效果可回写案例库（闭环）。

## 5 技术路线图

```
阶段1: 选题与方案设计
  ├── vibe coding 学习、选题调研、选题说明
  ├── 技术方向映射表、系统架构草图、数据来源确认
  └── git init、项目骨架、项目规则文件
         ↓
阶段2: 数据资源构建
  ├── 物料本体库设计(knowledge/material_ontology.json)
  ├── 仿真物料/BOM/历史案例生成脚本(seed=42 可复现)
  └── 数据入库(SQLite, 8 张业务表)
         ↓
阶段3: 算法模块开发(从零实现+单元测试)
  ├── rule_engine(JSON 规则表达式求值, 无 eval)
  ├── ontology_service(本体推理: 候选生成/约束过滤/推理路径)
  ├── cbr_service(案例推理: 加权 kNN 相似检索)
  └── fce_service(模糊综合评价: 隶属度/AHP+熵权/合成排序)
         ↓
阶段4: 系统开发(Vibe Coding)
  ├── 后端 API(FastAPI + SQLAlchemy, 6 个 router 约 28 端点)
  ├── 前端 SPA(Vue3 免构建, 6 个页面)
  ├── 前后端联调、e2e 闭环测试、前端挂载测试
  └── 截图工具、演示素材
         ↓
阶段5: 验收与展示
  ├── 需求规格说明书、设计报告(AI 使用披露)
  ├── 3 分钟演示视频录制(按演示脚本)
  └── 答辩 PPT 与答辩
```

## 6 计划安排

| 阶段 | 天数 | 对应日程 | 主要任务 |
|---|---|---|---|
| vibe coding 学习与选题调研 | 2 天 | D1-D2 | 提示词工程、上下文工程学习；完成选题 |
| 选题与方案设计 | 1 天 | D3 | 提交"选题说明.md"和"方案设计.md" |
| 数据资源整理 | 1 天 | D4 | 本体库设计、仿真数据生成脚本、数据入库 |
| 详细开发 | 3 天 | D5-D7 | 三算法模块、后端 API、前端页面开发 |
| 集成调试、报告撰写 | 1 天 | D8 | e2e 测试、撰写设计报告 |
| 答辩准备 | 1 天 | D9 | 演示视频录制、答辩 PPT |

## 7 参考文献

[1] 张智海等.《制造智能技术基础》. 北京：清华大学出版社，2022.
[2] 杨杰.《人工智能基础》. 北京：机械工业出版社，2025.
[3] Industrial Ontologies Foundry. IOF Core Ontology. https://github.com/iofoundry/Core
[4] ECLASS e.V. eCl@ss Classification and Product Description Standard. https://www.eclass.eu/
[5] Octopart. Common Parts Library Data. https://github.com/octopart/CPL-Data
[6] Kaggle. Engineering Blueprints & Bill of Materials Dataset. https://www.kaggle.com/datasets/devp1866/engineering-blueprints-and-bill-of-materials-dataset
[7] Aamodt A, Plaza E. Case-Based Reasoning: Foundational Issues, Methodological Variations, and System Approaches. AI Communications, 1994.
[8] Zadeh L A. Fuzzy Sets. Information and Control, 1965, 8(3): 338-353.
