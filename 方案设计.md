# 方案设计

> 题目：基于特征工程与 LSTM 的设备故障 PHM 预测系统
> 对应阶段：阶段 2 选题与方案规划
> 提交时间：2026-09-11

---

## 一、功能需求分析

### 1.1 业务背景

设备意外停机是制造企业的主要损失来源之一。预测性维护（PHM）通过传感器监测数据预测设备剩余使用寿命（RUL），提前安排维护窗口，实现"状态维修"替代"故障维修"。本系统以航空发动机（涡扇）为典型对象，但其方法可迁移至机床主轴、风机、压缩机等通用工业设备。

### 1.2 用户角色

| 角色 | 需求 |
|---|---|
| 设备维护工程师 | 上传设备传感器数据，获取 RUL 预测与健康等级，安排维护计划 |
| 生产管理者 | 查看设备健康趋势与历史预测记录，掌握设备风险概况 |

### 1.3 功能需求列表

| 编号 | 功能 | 描述 | 优先级 |
|---|---|---|---|
| F1 | 数据上传 | 支持上传传感器时序数据文件（CSV/TXT） | 高 |
| F2 | RUL 预测 | 调用特征工程 + LSTM 模型，返回 RUL 预测值（飞行循环数） | 高 |
| F3 | 健康状态评估 | 将 RUL 映射为健康/退化/危险/失效四级并给出维护建议 | 高 |
| F4 | 趋势可视化 | 展示传感器原始趋势与关键特征曲线（ECharts 图表） | 中 |
| F5 | 历史记录 | 预测记录持久化存储（SQLite），支持查询 | 中 |
| F6 | 基线对比 | 随机森林基线模型预测结果对照展示 | 低 |

### 1.4 非功能需求

- **可用性**：一键启动（start.bat），本地浏览器访问；
- **性能**：单次预测响应时间 < 3 秒（CPU 推理）；
- **正确性**：特征工程与推理管线通过单元测试与接口测试；
- **可追溯**：全程 git 提交，AI 对话记录存于 `prompt/`。

## 二、方案论证

### 2.1 算法选型论证：为什么用 LSTM

| 候选方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 传统机器学习（随机森林/XGBoost） | 简单、训练快、可解释 | 需手工构造特征，难建模时序依赖 | 作为基线模型保留 |
| LSTM（循环神经网络） | 天然建模时序依赖，PHM 领域经典方法，效果经文献验证 | 训练较慢、需调参 | **选作核心模型** |
| Transformer | 效果强 | 数据量小易过拟合，部署重 | 不选 |

论证结论：C-MAPSS 数据为多变量时间序列，RUL 预测依赖传感器值在时间上的退化趋势，LSTM 的"记忆门控"机制适合捕获这种长期依赖；同时保留随机森林作为对照基线，可量化深度学习的增益，也满足课程"机器学习"技术方向覆盖。

### 2.2 架构选型论证：为什么 B/S + FastAPI + SQLite

| 选型 | 理由 |
|---|---|
| B/S 架构 | 任务书要求完整 B/S 或 C/S；B/S 免安装客户端，演示方便，浏览器即可访问 |
| FastAPI | 轻量、自带 OpenAPI 文档、异步支持、类型校验（Pydantic），适合课程规模后端 |
| SQLite | 零配置单文件数据库，满足课程设计数据量，避免 MySQL/PostgreSQL 安装负担 |
| 前端原生 HTML+JS+ECharts | 不引入前端构建链（Node 等），降低部署复杂度，图表用 ECharts 快速实现 |

### 2.3 特征工程方案论证

C-MAPSS 原始数据 21 个传感器中部分传感器（如温度/压力静态值）不随退化变化，直接入模会引入噪声。方案：

1. **传感器筛选**：计算各传感器与 RUL 的相关性（Pearson 相关系数），保留 |r| 高于阈值的传感器；
2. **滑动窗口特征**：对每个飞行循环，以过去 N 个循环为窗口，构造均值、标准差、最大值、斜率等统计特征；
3. **归一化**：Min-Max 归一化到 [0,1]，消除量纲差异。

## 三、方案规划

### 3.1 技术栈

| 层 | 技术 |
|---|---|
| 前端 | HTML5 + CSS3 + JavaScript + ECharts |
| 后端 | Python 3.12 + FastAPI + Uvicorn |
| 数据库 | SQLite（sqlite3 / SQLAlchemy 可选） |
| 算法 | PyTorch（LSTM）、scikit-learn（随机森林、特征处理） |
| 测试 | pytest + httpx（接口测试） |
| 版本管理 | git + GitHub |
| AI 开发工具 | 豆包 AI 助手（vibe coding），备选 Trae / Deepseek Harness |

### 3.2 需配置的外部资源

| 资源 | 用途 | 状态 |
|---|---|---|
| Python 3.12（Anaconda） | 运行环境 | ✅ 已就绪 |
| PyTorch（CPU 版） | LSTM 训练与推理 | ✅ 已就绪 |
| FastAPI / Uvicorn / pytest | Web 框架与测试 | ✅ 已安装 |
| GitHub 仓库 | 代码托管与过程考核 | ✅ 已创建 |
| NASA C-MAPSS 数据集 | 模型训练数据 | ✅ 已下载 |

### 3.3 数据集来源

- **NASA C-MAPSS**：https://data.nasa.gov/dataset/cmapss-jet-engine-simulated-data
- 使用 FD001 子集：训练 100 台发动机（20631 条记录）、测试 100 台（13096 条记录），每条记录含 3 个工况参数 + 21 个传感器值；
- 原始文件存放：`data/raw/`；预处理后数据存放：`data/processed/`；
- 数据说明文件：`data/说明.md`。

### 3.4 前端 UI 方案

单页面应用（index.html），布局：

- **顶部**：系统标题与简介；
- **上传区**：选择传感器数据文件（CSV/TXT），点击"开始预测"；
- **结果区**：RUL 预测值卡片、健康等级徽章、维护建议；
- **图表区**：传感器原始趋势折线图、关键特征分布图（ECharts）；
- **历史区**：预测记录表格（时间、单元、RUL、健康等级），支持刷新。

### 3.5 后端与算法方案

```
backend/
├── app.py                 # FastAPI 应用与路由
├── schemas.py             # Pydantic 数据模型
├── database.py            # SQLite 初始化与操作
├── services/
│   ├── predictor.py       # 预测编排（特征工程→模型→评估→入库）
│   └── health_eval.py     # 健康等级与维护建议
└── models/
    ├── feature_engineering.py  # 传感器筛选+滑动窗口特征+归一化
    ├── lstm_model.py           # LSTM 网络定义与推理
    └── ml_baseline.py          # 随机森林基线
```

**算法管线**：原始时序 → 传感器筛选 → 滑动窗口特征 → 归一化 → LSTM 推理 → RUL 输出 → 健康等级评估 → 写入数据库 → 返回前端。

## 四、技术路线

```
阶段1 学习与调研 ──► 阶段2 选题与方案设计
      │                    │
      ▼                    ▼
阶段3 数据准备 ◄──── 数据下载（C-MAPSS）→ 预处理 → data/processed
      │
      ▼
阶段4 详细开发
  ├─ 算法模块：特征工程 → LSTM 训练（train.py）→ 模型保存 → 测试
  ├─ 后端：FastAPI 路由 + SQLite 持久化 → 接口测试
  └─ 前端：页面 + ECharts 可视化 → 联调
      │
      ▼
阶段5 集成调试：pytest 全量测试 → 一键启动 → 演示验证
      │
      ▼
阶段6 文档与答辩：设计报告 / 演示视频 / 答辩 PPT
```

## 五、计划安排

| 阶段 | 任务 | 计划时间 | 产出 |
|---|---|---|---|
| 1 | AI 工具学习、git 学习、选题调研 | D1-D2 | 学习笔记.md、GitHub 仓库 |
| 2 | 选题与方案设计 | D3-D5 | 选题说明.md、方案设计.md |
| 3 | 数据下载与预处理 | D6 | data/raw、data/processed、预处理脚本 |
| 4 | 算法开发与训练 | D7-D8 | 特征工程、LSTM 模型、随机森林基线、测试 |
| 5 | 前后端开发与集成 | D7-D8 | 后端 API、前端 UI、SQLite 入库 |
| 6 | 集成调试 | D9 | pytest 全量通过、一键启动、演示 |
| 7 | 报告与答辩 | D9-D10 | 设计报告、演示视频、答辩 PPT |

## 六、参考文献

[1] 张智海 等. 制造智能技术基础[M]. 北京: 清华大学出版社, 2022.（技术方向知识支撑：机器学习、深度学习、数据预处理）

[2] 杨杰. 人工智能基础[M]. 北京: 机械工业出版社, 2025.

[3] Saxena A, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation[C]. International Conference on Prognostics and Health Management, 2008.（C-MAPSS 数据集原始论文）

[4] Hochreiter S, Schmidhuber J. Long short-term memory[J]. Neural Computation, 1997, 9(8): 1735-1780.（LSTM 原始论文）

[5] NASA Prognostics Center of Excellence. C-MAPSS Turbofan Engine Degradation Simulation Data Set[DB/OL]. https://data.nasa.gov/dataset/cmapss-jet-engine-simulated-data

[6] Heimes F O. Recurrent neural networks for remaining useful life estimation[C]. International Conference on Prognostics and Health Management, 2008.（RNN 用于 RUL 估计的经典工作）
