# 选题说明

| 项目 | 内容 |
|---|---|
| 题目 | 基于特征工程与 LSTM 的设备故障 PHM 预测系统 |
| 姓名/学号 | （填写班级.学号.姓名） |
| 提交时间 | 2026-09-11 |

---

## 一、拟定题目

**基于特征工程与 LSTM 的设备故障 PHM 预测系统**

以航空发动机退化数据为对象，基于传感器多变量时间序列，利用特征工程与长短期记忆网络（LSTM）构建剩余使用寿命（RUL）预测模型，并通过 B/S 架构 Web 系统实现数据上传、寿命预测、健康状态评估与维护建议的完整业务闭环。

## 二、完成目标

1. **功能目标**：实现一个可运行的 B/S 架构演示系统，用户上传设备传感器监测数据后，系统返回：
   - 剩余使用寿命（RUL）预测值；
   - 健康状态等级（健康 / 退化 / 危险 / 失效）；
   - 传感器趋势分析与风险预警；
   - 历史预测记录查询。
2. **技术目标**：完整落地"前端 UI + 后端 API + 数据库 + 算法模块"四层架构；算法模块覆盖至少 3 个课程技术方向并真实参与业务闭环。
3. **工程目标**：全程 git 管理、小步提交；配备自动化测试（pytest）；数据、模型、代码、文档齐全可复现。

## 三、技术方向及映射

本选题覆盖 **4 个**上学期课程专题技术方向，每个方向在系统中均有实际作用（非装饰性引入）：

| 序号 | 技术方向 | 课程专题 | 在系统中的实际作用 |
|---|---|---|---|
| 1 | 数据预处理与特征工程 | 制造数据处理 / 特征提取 | 传感器筛选（去除退化信息弱的传感器）、滑动窗口统计特征构造（均值/标准差/斜率）、归一化，为建模提供有效输入 |
| 2 | 机器学习回归 | 机器学习基础 | 随机森林（RF）回归作为基线模型，与 LSTM 结果对比，验证深度学习方法有效性 |
| 3 | 深度学习——循环神经网络（LSTM） | 深度学习 / 循环神经网络 | 核心预测模型，捕捉传感器时序长期依赖，输出 RUL 预测值 |
| 4 | 健康状态评估与决策 | 智能状态监测 / 设备管理 | 将 RUL 映射为健康等级与维护建议（预警、检修、停运），支撑维护决策 |

## 四、系统架构草图

```
┌─────────────────────────────────────────────────────────┐
│                    前端（B/S 浏览器端）                     │
│  数据上传 / 预测结果展示 / 传感器趋势图 / 历史记录查询      │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTP (JSON)
┌──────────────────────────▼──────────────────────────────┐
│              后端服务层（FastAPI）                        │
│  /api/predict  预测接口（特征工程→LSTM→健康评估→入库）    │
│  /api/history  历史记录查询接口                          │
│  /api/units    训练集单元信息接口                        │
└──────────┬──────────────────────────┬───────────────────┘
           │                          │
┌──────────▼──────────┐   ┌──────────▼───────────────────┐
│   算法模块（Python）  │   │    数据库（SQLite）          │
│ ①特征工程模块        │   │  predictions 预测记录表      │
│ ②LSTM 预测模型       │   │  units 训练单元信息表        │
│ ③随机森林基线模型    │   │                              │
│ ④健康状态评估模块    │   │                              │
└─────────────────────┘   └──────────────────────────────┘
```

## 五、数据来源

| 项目 | 内容 |
|---|---|
| 数据集 | NASA C-MAPSS 涡扇发动机退化仿真数据集 |
| 来源 | NASA 公开数据门户（Prognostics Center of Excellence） |
| 链接 | https://data.nasa.gov/dataset/cmapss-jet-engine-simulated-data |
| 备份链接 | https://ti.arc.nasa.gov/tech/dash/groups/pcoe/prognostic-data-repository/ |
| 使用子集 | FD001（单工况，训练 100 台 / 测试 100 台发动机，特征：3 工况 + 21 传感器） |
| 说明 | 公开数据集，符合任务书"公开工业数据集"要求；原始文件已归档至仓库 `data/raw`，预处理产物见 `data/processed` |

## 六、选题可行性自检

| 任务书要求 | 本选题落实情况 |
|---|---|
| 场景为制造/工业环节 | ✅ 设备运维（预测性维护） |
| 至少 3 个课程技术方向 | ✅ 4 个（特征工程、机器学习、深度学习 LSTM、健康评估） |
| 完整 B/S 架构 | ✅ 前端 + FastAPI 后端 + SQLite + 算法模块 |
| 真实业务感 | ✅ 上传监测数据→预测寿命→健康评估→维护建议，闭环完整 |
| 数据可获得 | ✅ NASA 公开数据集，已验证可下载 |
